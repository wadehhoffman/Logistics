# Local secrets — never commit
secrets.js

# Excel temp files
~$*.xlsx
~$*.xls

# macOS
.DS_Store

# Node
node_modules/

# iOS / Xcode
CarterLumberRoutes/CarterLumberRoutes.xcodeproj/xcuserdata/
CarterLumberRoutes/CarterLumberRoutes.xcodeproj/project.xcworkspace/xcuserdata/
CarterLumberRoutes/DerivedData/
CarterLumberRoutes/.build/
*.xcuserstate

# Server-local state (per-deployment data, not repo content)
schedule.json
mills.json
yards.json
activity.json
